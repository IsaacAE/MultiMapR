% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_render.R
\name{dibujar_filograma_canvas}
\alias{dibujar_filograma_canvas}
\title{Draws a phylogram using the "Blank Canvas" strategy
with geometrically isotropic offset (X + Y) to visually separate
multi-mappings.}
\usage{
dibujar_filograma_canvas(
  filogenia,
  lista_ec,
  grosor = 2,
  label_offset = 0.3,
  x_limit = NULL,
  desfases_y = NULL
)
}
\arguments{
\item{filogenia}{phylo object.}

\item{lista_ec}{List of color vectors. Each vector has length
\code{nrow(filogenia$edge)}.}

\item{grosor}{Line width (lwd).}

\item{label_offset}{Distance between tips and labels.}

\item{x_limit}{Right limit of X axis (NULL = automatic).}

\item{desfases_y}{Numeric vector of Y offsets (one per history).
If NULL, \code{seq(-0.08, 0.08, length.out = N)} is used.}
}
\value{
Invisible NULL. Draws on the active graphics device.
}
\description{
Accepts a list of color vectors (one per evolutionary history).
If the list has 1 element -> simple mapping (no offset).
If it has N > 1 -> multi-mapping with zero-centered offset:
· The user only provides \code{desfases_y} (vertical lanes).
· \code{desfases_x} is calculated automatically just after
\code{plot(..., type = "n")}, when the graphics engine already knows
the canvas dimensions. The calculation guarantees that the physical
separation (in inches) between verticals is identical to the
physical separation between horizontals, regardless of window size
or tree length:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{  usr      <- par("usr")   # data coord limits: c(x1,x2,y1,y2)
  pin      <- par("pin")   # physical dimensions in inches: c(w, h)
  escala_x <- (usr\[2\] - usr\[1\]) / pin\[1\]   # data units / inch on X
  escala_y <- (usr\[4\] - usr\[3\]) / pin\[2\]   # data units / inch on Y
  desfases_x <- desfases_y * (escala_x / escala_y)
}\if{html}{\out{</div>}}

Horizontal strokes are vectorized with a single \code{segments()}.
Vertical strokes (internal node segments) are drawn by iterating over
each internal node, using the \emph{incoming} edge color to guarantee a solid,
uniform color. \code{lend = 1} ensures perfectly square joins between
horizontals and verticals.
The vertical stroke is protected with \verb{if (y_min != y_max)} to avoid
collisions in polytomies and zero-length branches.
}
