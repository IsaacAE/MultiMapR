% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_multimapr_tree.R
\name{.emtree_dibujar_leyenda}
\alias{.emtree_dibujar_leyenda}
\title{Dibuja la leyenda en la esquina reservada del margen}
\usage{
.emtree_dibujar_leyenda(
  corner,
  legend_by_char = NULL,
  legend_labels = NULL,
  legend_colors = NULL,
  legend_title = NULL,
  pch = 15,
  cex_ley = 0.8
)
}
\arguments{
\item{corner}{String de esquina ("topleft", etc.)}

\item{legend_by_char}{Lista nombrada carácter → list(labels, colors). Si no
es NULL tiene precedencia sobre \code{legend_labels}.}

\item{legend_labels}{Vector plano de etiquetas (modo legacy).}

\item{legend_colors}{Vector plano de colores (modo legacy).}

\item{legend_title}{Título del bloque único (modo legacy, string o NULL).}

\item{pch}{Símbolo de leyenda (default 15 = cuadrado relleno).}

\item{cex_ley}{Tamaño de fuente para la leyenda.}
}
\description{
Soporta dos modos:
\itemize{
\item \strong{Agrupado} (\code{legend_by_char} no NULL): recibe una lista nombrada
carácter → list(labels, colors) y dibuja cada carácter como un bloque
independiente con su encabezado en negrita, sin mezclar estados.
\item \strong{Plano} (legacy): usa \code{legend_labels} + \code{legend_colors} + \code{legend_title}
como antes, para mantener compatibilidad con llamadas antiguas.
}
}
\details{
Se llama \strong{después} de renderizar el árbol. Usa \code{par(xpd = NA)} para
poder pintar fuera del área de graficado pero dentro del dispositivo.
}
