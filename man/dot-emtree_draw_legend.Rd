% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_multimapr_tree.R
\name{.emtree_draw_legend}
\alias{.emtree_draw_legend}
\title{Draws the legend in the reserved corner of the margin}
\usage{
.emtree_draw_legend(
  corner,
  legend_by_char = NULL,
  legend_labels = NULL,
  legend_colors = NULL,
  legend_title = NULL,
  pch = 15,
  cex_ley = 0.8
)
}
\arguments{
\item{corner}{Corner string ("topleft", etc.)}

\item{legend_by_char}{Named list character → list(labels, colors). If not
NULL takes precedence over \code{legend_labels}.}

\item{legend_labels}{Flat label vector (legacy mode).}

\item{legend_colors}{Flat color vector (legacy mode).}

\item{legend_title}{Single block title (legacy mode, string or NULL).}

\item{pch}{Legend symbol (default 15 = filled square).}

\item{cex_ley}{Font size for the legend.}
}
\description{
Supports two modes:
\itemize{
\item \strong{Grouped} (\code{legend_by_char} not NULL): receives a named list
character → list(labels, colors) and draws each character as an
independent block with its header in bold, without mixing states.
\item \strong{Flat} (legacy): uses \code{legend_labels} + \code{legend_colors} + \code{legend_title}
as before, to maintain compatibility with older calls.
}
}
\details{
Called \strong{after} rendering the tree. Uses \code{par(xpd = NA)} to
paint outside the plot area but within the device.
}
