% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_render.R
\name{plot_simple_mapping}
\alias{plot_simple_mapping}
\title{SIMPLE MAPPING — neutral tree + colored figure table/rings at terminals}
\usage{
plot_simple_mapping(filogenia, config)
}
\arguments{
\item{filogenia}{phylo object.}

\item{config}{Configuration list from setup_mapping_config().}
}
\value{
Invisible NULL. Draws on the active graphics device.
}
\description{
For phylogram and cladogram:
\itemize{
\item Draws the tree with x.lim expanded to accommodate the table and legend.
\item The colored figure table appears on the right with the full character
name as a column header (rotated 45°).
\item The legend is positioned in the chosen corner, one block per character.
For fan:
\item Concentric rings of symbols around the tree.
\item C1, C2... abbreviations above each ring (once, anchored at the
terminal with the largest angle).
\item Tip labels attached to the last ring, parallel to the radius.
\item Legend in blocks, same as multi-mapping mode.
}
}
\details{
Spacing strategy (phylogram / cladogram):
Phase 1 — off-screen probe: the tree is drawn in pdf(NULL) with the same
dimensions as the final file to obtain real par("usr") and par("pin").
With those values the following are calculated:
a) x_max_tabla  = x of the last column + horizontal projection of the
longest header rotated 45°
b) x_max_leyenda = x_max_tabla + legend width measured with
legend(..., plot=FALSE)
If x_max_leyenda > \code{usr[2]}, x.lim is expanded by the necessary amount
(in data units) and passed as x.lim to the definitive plot().
Phase 2 — definitive render: the final device is opened and everything
is drawn inside the already-expanded viewport.
}
